# Deploy on Vercel

We deploy the Next.js app on Vercel and attach a custom domain, rather than self-hosting it on the developer's own machine behind a tunnel. Vercel builds and runs Next.js directly, provides HTTPS and domain wiring for free, and needs no machine left running. The trade-off is vendor lock-in: moving to self-hosting later means rebuilding the deployment setup.
